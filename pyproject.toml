[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "autopr"
version = "0.1.0"
description = "Autonomous agent that finds open-source bounties, writes fixes, and submits PRs"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "openai>=1.30.0",
    "fastapi>=0.110.0",
    "uvicorn>=0.29.0",
    "httpx>=0.27.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "anyio>=4.0.0",
    "aiofiles>=23.0.0",
    "sse-starlette>=1.8.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
mcp = ["mcp>=1.2.0"]
slack = ["mcp>=1.2.0", "slack_bolt>=1.18", "aiohttp>=3.9"]

[project.scripts]
autopr = "autopr.cli:main"
autopr-mcp = "autopr.mcp_server:main"
autopr-slack = "slack_app.app:main"
autopr-api = "autopr.api_server:main"

[tool.hatch.build.targets.wheel]
packages = ["autopr", "slack_app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
